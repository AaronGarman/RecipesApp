HIGH
- Borders + backgrounds + UI elements adjust, colors? sizes, fonts/sizes, colors, placements, styling, corner radius + borders
- Recheck code all - comments in + out, more extensions + functions

MEDIUM
- Constraints all - stack views
- Keyboard down - after type, tap off, enter (newline for dirs), keyboard up methods or scrollview or push up screen, for no block typing too? End editing like IBAction in ios102 lab 2? Constraints on box width to keep text format? for dirs, enter to end?

LOW
- User auth
- Parse Database - create, update, delete - worst case just do user defaults

SMALL
- Maybe navigation title do recipe name rather than below on detail? or keep as is, or no nav title. image or name on top?
- mock directions?
- Aspect fill all images
- default image
- numeric keyboard mins?

----------

OPTIONAL
- Filter button for list (all, food, drink, dessert)
- splash screen + app logo
- fix constraint warnings
- maybe directions as indiv strings n tableview? like bullets?
- Empty holder text on both tableviews
- redo any constraints future? study more how w/ sizes of images n text views?

GENERAL
- comments in + out
- print out data? print v logs?
- org code at end
- folders for files? sep folders tableview cells?
- no need reload data in view will appear? would be for after add?
- gpt ask on comments stuff, like if need something


Look into taking out not number alert
Default image
Error check mins > 0?
