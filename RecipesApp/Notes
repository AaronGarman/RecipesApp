HIGH
- Camera pull down button for options
- Borders + backgrounds + UI elements adjust, colors?
- Recheck code all - comments in + out

MEDIUM
- Constraints all - stack views
- Keyboard down after type or click off, keyboard no block screen too - scroll view? end editing like IBAction in ios102 lab 2?

LOW
- User auth
- Parse Database - create, update, delete - worst case just do user defaults

SMALL
- Maybe navigation title do recipe name rather than below on detail? or keep as is, or no nav title. image or name on top?
- mock directions?
- Aspect fill all images
- more extensions n functions?
- default image

----------

OPTIONAL
- Filter button for list (all, food, drink, dessert)
- splash screen + app logo
- fix constraint warnings
- maybe directions as indiv strings n tableview?
- Empty holder text on both tableviews

GENERAL
- comments in + out
- print out data? print v logs?
- org code at end
- folders for files? sep folders tableview cells?
- no need reload data in view will appear? would be for after add?







HIGH
- pop up button for 2 pic options - take off green highlight, both button references remove? also button reg ref remove - 3 ref total rid
- adjust tableview + ui elements based on mock data - row height, fonts, font sizes, ui element placements, etc. fonts bold + sizes? corner radius image view on add recipe? numeric keyboard mins? default n/a pic

MEDIUM
- keyboard down after type + no block entry - scrollview or diff? also tap out of box keyboard down too?

LOW
- fig text into directions - press enter? bullets? or add indiv directions or parse directions either for array of strings? or upload image of directions? Also add directions into mock data - consraints on box width to keep text format?

Allow user click off text boxes n also return, but for directions do enter for newline
Return for newline, but as stop w others
Tap off too close keyboard
Keyboard up methods or scrollview

Button highlight out n some stuff, others keep. Delete button reference
